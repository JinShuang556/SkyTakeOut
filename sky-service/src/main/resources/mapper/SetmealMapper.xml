<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.qrs.mapper.SetmealMapper">
    <!--分页查询-->
    <select id="page" resultType="com.qrs.vo.SetmealPageVO">
        SELECT
            s.id,
            s.name,
            s.category_id,
            s.price,
            s.status,
            s.description,
            s.image,
            s.update_time,
            c.name as categoryName
        FROM setmeal as s
        LEFT JOIN category as c ON s.category_id = c.id
        <where>
            <if test="name != null and name != ''"> AND s.name LIKE CONCAT('%',#{name},'%')</if>
            <if test="categoryId != null"> AND s.category_id = #{categoryId}</if>
            <if test="status != null"> AND s.status = #{status}</if>
        </where>
    </select>

    <!--新增套餐-->
    <insert id = "insert" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO setmeal(name,category_id,price,status,description,image,create_time,update_time,create_user,update_user)
        VALUE (#{name},#{categoryId},#{price},#{status},#{description},#{image},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>

    <!--批量删除套餐-->
    <delete id="deleteBatch">
        DELETE FROM setmeal WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <if test="ids == null"> AND 1 = 0</if>
    </delete>

    <!--定义结果集-->
    <resultMap id="setmealWithSetmealDishMap" type="com.qrs.vo.SetmealWithSetmealDishVO">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="category_id" property="categoryId"/>
        <result column="price" property="price"/>
        <result column="status" property="status"/>
        <result column="description" property="description"/>
        <result column="image" property="image"/>
        <result column="update_time" property="updateTime"/>
        <result column="category_name" property="categoryName"/>
        <collection property="setmealDishes" ofType="com.qrs.entity.SetmealDish">
            <id column="sd_id" property="id"/>
            <result column="sd_setmeal_id" property="setmealId"/>
            <result column="sd_dish_id" property="dishId"/>
            <result column="sd_name" property="name"/>
            <result column="sd_price" property="price"/>
            <result column="sd_copies" property="copies"/>
        </collection>
    </resultMap>

    <!--根据套餐id查询套餐和套餐关联菜品-->
    <select id="selectSetmealWithSetmealDishById" resultMap="setmealWithSetmealDishMap">
        SELECT
            s.id,
            s.name,
            s.category_id,
            s.price,
            s.status,
            s.description,
            s.image,
            s.update_time,
            c.name as category_name,
            sd.id as sd_id,
            sd.setmeal_id as sd_setmeal_id,
            sd.dish_id as sd_dish_id,
            sd.name as sd_name,
            sd.price as sd_price,
            sd.copies as sd_copies
        FROM setmeal as s
        LEFT JOIN category as c ON s.category_id = c.id
        LEFT JOIN setmeal_dish as sd ON s.id = sd.setmeal_id
        WHERE s.id = #{id}
    </select>

    <!--修改套餐-->
    <update id="updateSetmealById">
        UPDATE setmeal
        <set>
            <if test="name != null || name != ''">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="status != null">status = #{status},</if>
            <if test="description != null || description != ''">description = #{description},</if>
            <if test="image != null || image != ''">image = #{image},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        Where id = #{id}
    </update>
</mapper>